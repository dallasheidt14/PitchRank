# Generated by frontend/scripts/generate-llms-txt.ts; CI runs the generator on
# Linux and compares with `git diff --exit-code`. Pin to LF so Windows
# core.autocrlf=true contributors don't flap the CI drift check.
public/llms.txt text eol=lf
