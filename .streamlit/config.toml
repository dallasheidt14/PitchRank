[theme]
primaryColor = "#1f77b4"
backgroundColor = "#FFFFFF"
secondaryBackgroundColor = "#f0f2f6"
textColor = "#262730"
font = "sans serif"

[server]
headless = true
port = 8501
# Note: enableCORS and enableXsrfProtection are mutually exclusive
# Using Streamlit defaults for cloud deployment compatibility

[browser]
gatherUsageStats = false
