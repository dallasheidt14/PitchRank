## /pr - Create Pull Request

1. Confirm current branch is NOT main. If on main, create a feature branch first.
2. Stage and commit all changes with a conventional commit message.
3. Push the branch to origin.
4. Create a PR using `gh pr create --fill`.
5. Report the PR URL.
