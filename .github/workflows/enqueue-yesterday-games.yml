name: Enqueue Yesterday-Game Teams
run-name: >-
  Enqueue yesterday teams · ${{
    inputs.dry_run == true && 'DRY RUN' || 'live'
  }}

on:
  schedule:
    # 07:13 UTC = ~1am MDT / midnight MST (Mountain). Runs overnight so the
    # process-missing-games drain (every 15min) has hours to clear the queue
    # before morning. GH Actions cron is UTC-only, so this drifts ±1h with DST.
    #
    # Minute 13, not 0. Scheduled runs are best-effort and are dropped when
    # GitHub's scheduler is busy, and minute 0 is the most contended slot on the
    # platform. On minute 0 this drifted to 17:56 on 2026-08-27 and was skipped
    # entirely on 2026-08-28, which emptied the queue and stopped scraping.
    # process-missing-games already runs at 7,22,37,52 for the same reason.
    - cron: '13 7 * * *'
  workflow_dispatch:
    inputs:
      dry_run:
        description: 'Dry run: log targets without enqueueing'
        required: false
        type: boolean
        default: false

concurrency:
  group: enqueue-yesterday-games
  cancel-in-progress: false

jobs:
  enqueue:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      DRY_RUN_FLAG: ${{ inputs.dry_run == true && '--dry-run' || '' }}
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install supabase python-dotenv truststore certifi

      - name: Enqueue yesterday-game teams
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
        run: |
          export PYTHONPATH="${PYTHONPATH}:${PWD}"
          python scripts/enqueue_yesterday_games.py $DRY_RUN_FLAG
