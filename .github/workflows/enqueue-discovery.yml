name: Enqueue Discovery Teams
run-name: >-
  Enqueue discovery · ${{
    inputs.dry_run == true && 'DRY RUN' || 'live'
  }}

on:
  schedule:
    # Sunday 14:41 UTC. After the weekend yesterday-game enqueue spike has had
    # time to drain (that daily enqueue runs at 07:13 UTC).
    #
    # Minute 41, not 0: minute 0 is the most contended slot on GitHub's
    # scheduler and scheduled runs are dropped under load. The daily enqueue
    # jobs on minute 0 drifted ~10h on 2026-08-27 and were skipped on 08-28.
    - cron: '41 14 * * 0'
  workflow_dispatch:
    inputs:
      dry_run:
        description: 'Dry run: log targets without enqueueing'
        required: false
        type: boolean
        default: false
      limit:
        description: 'Max teams to enqueue (blank = 1000)'
        required: false
        type: string
        default: ''

concurrency:
  group: enqueue-discovery
  cancel-in-progress: false

jobs:
  enqueue:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      DRY_RUN_FLAG: ${{ inputs.dry_run == true && '--dry-run' || '' }}
      LIMIT_FLAG: ${{ inputs.limit != '' && format('--limit {0}', inputs.limit) || '' }}
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install supabase python-dotenv

      - name: Enqueue discovery teams
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
        run: |
          export PYTHONPATH="${PYTHONPATH}:${PWD}"
          python scripts/enqueue_discovery_teams.py $DRY_RUN_FLAG $LIMIT_FLAG
