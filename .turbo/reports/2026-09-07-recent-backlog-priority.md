# Backlog noted 2026-08-24 → 2026-09-07, ranked

84 entries were noted in this window. Six of the whole backlog closed in the
2026-09-07 sweep (none of them from this window except IMP-193), leaving **81 open
and 2 deferred** here.

Ranked by what the defect costs while it stays open, not by how interesting it is.
Effort is **S** (one file, under ~10 lines), **M** (one PR, several files or a
migration), **L** (needs its own plan).

Every P1 and P2 entry below was re-verified against live code on 2026-09-07 — the
backlog's own prose was wrong about two of them (IMP-110, IMP-172), so nothing here
is taken on trust.

---

## P1 — Wrong data is reaching people right now

| # | ID | What is wrong | Effort |
|---|-----|---------------|--------|
| 1 | IMP-114 | Downloadable social graphics name real teams next to rank changes generated by `Math.floor(Math.random()*15)-7`. The real `rank_change_7d/30d` is already on the row. Fabricated public output. | S |
| 2 | IMP-128 | Every team's games-played / wins / losses / draws has been frozen for months. The RPC is cancelled on every run and the Python fallback has never written a row; the diagnosed cause is that its `SET LOCAL statement_timeout` was never in force. | M |
| 3 | IMP-134 | The duplicate scan pages without `.order()`, so ~16% of every cohort is never fetched — 26,756 rows ordered vs 22,508 unordered on u19. Those teams appear in no count and no verdict, so every duplicate figure ever produced is a floor. | S |
| 4 | IMP-136 | `_GENDER_WORD` holds literal `0x08` bytes where `\b` was intended. Confirmed live: `birth_years('Club 12 Boys')` returns nothing. 2,953 rows therefore state no birth year, which disarms the one guard stopping a 2008 team absorbing a 2009 team in a merge. | S |
| 5 | IMP-168 | `scrape_requests` policies are `FOR INSERT TO public WITH CHECK (true)` with `anon=arwdDxtm`. Anyone holding the public site key can queue priority-1 paid scrapes. The RPC above it is locked down; the table is not. | M |
| 6 | IMP-172 | A `Male U18` roster heading resolves to `u18`, and PitchRank holds zero `u18` teams, so every U18 division pasted into the Seeding tab matches an empty cohort. The entry claimed this shipped in #1081 — it did not. | S |
| 7 | IMP-184 | 2,192 live TGS teams (~96% of all blank `state_code`) have no assignment path at all, so they are ranked but absent from every state board, and the count grows each Monday. Ongoing fix costs zero extra API calls; backfill is 558 one-off calls. | L |

## P2 — Silently corrupting or losing data on every run

| # | ID | What is wrong | Effort |
|---|-----|---------------|--------|
| 8 | IMP-132 | `process_missing_games` stamps `last_scraped_at` after scraping only a ±90-day window, but that timestamp is also the incremental watermark. For a never-scraped team, history older than 90 days becomes unreachable while still inside the 365-day ranking window. | M |
| 9 | IMP-106 | Teams with no `rankings_full` row violate the `age_group` NOT NULL constraint and kill whole 500-row batches, so existing teams in those batches keep stale stats. Pairs with #2. | S |
| 10 | IMP-137 | The 2,135 existing merge-created duplicate fixtures were repaired by hand. Prevention is still open, so the next merge batch recreates them. Two hard-won findings are recorded in the entry — use them. | M |
| 11 | IMP-138 | Modular11 files some U13 fixtures onto the U14 team row, inflating that row's record and making duplicate detection fire on legitimately distinct teams. | L |
| 12 | IMP-161 | A club stored 2 teams in one state and 2 in another has every team proposed as a correction, so applying the sweep recreates the split and the club oscillates forever. Observed on RSL-AZ Yuma and Amigos FC. | S |
| 13 | IMP-143 | Every component of the unknown-opponent approval gate is spelled `!= "mismatch"`, so a provider outage makes it approve rather than abstain — and the writes it authorizes touch immutable `games` rows. | M |
| 14 | IMP-165 | An operator's approved state is stamped `tier_<x>`, so the provenance gate cannot tell it from an automatic fill and a later sweep overwrites it unreported. 98 rows carry that stamp. | M |
| 15 | IMP-163 | UK and European touring clubs hold US states and bounce between two of them every sweep; 17 more teams carry codes that are not a state or province at all. None belongs on a state board. Needs a product decision first. | L |
| 16 | IMP-164 | GotSport answers Canadian teams with their province (83 of 2,545 probes) and the map discards all of them, so the paid answer is thrown away and the club count decides instead — wrongly, for AS Gatineau. | S |

## P3 — Reports the wrong outcome to an operator

| # | ID | What is wrong | Effort |
|---|-----|---------------|--------|
| 17 | IMP-133 | Data-hygiene Step 1b goes red whenever its grep finds nothing, because the assignment sits under `set -o pipefail` and the step ends there. A run whose backfill succeeded reports failure. | S |
| 18 | IMP-140 | A dry run of the club/state chain prints `Updated: 43`, identical to a live run, and that line is what the Pipeline Summary surfaces. Verifying nothing was written costs a read of two scripts every time. | S |
| 19 | IMP-107 | Every weekly ranking run opens with `SUPABASE_KEY is not set — database calls will fail` while running fine on the service-role key, training readers to ignore real credential errors. | S |
| 20 | IMP-169 | The docstring claims the enqueue RPC preserves `game_date` on an existing pending row. It does not, and this is the comment that would talk the next person out of a protection two other scripts depend on. | S |
| 21 | IMP-180 | The seeding review CSV carries provider-authored team names with no formula-prefix guard, so a team registered as `=WEBSERVICE(...)` runs the moment an operator opens the file. | S |
| 22 | IMP-146 | A due-diligence query batches 500 ids against the documented 100-id URI limit. Works until it doesn't, then fails as request-too-long rather than a partial result. | S |
| 23 | IMP-173 | The GotSport event landing page fetch never sets `response.encoding` and the server declares no charset, so accented and non-Latin text decodes as mojibake. Same one-line fix already applied to the roster fetch. | S |

## P4 — Security hardening, not reachable today

| # | ID | What is wrong | Effort |
|---|-----|---------------|--------|
| 24 | IMP-152 | `anon` holds `arwdDxtm` on `team_state_audit`, `team_state_review_queue` and `tgs_events`. RLS does not govern TRUNCATE, and `team_state_audit` is the append-only ledger `revert_team_states` depends on. | S |
| 25 | IMP-153 | `anon` retains DELETE and TRUNCATE on `user_profiles`, which holds the Stripe subscription state. Not reachable through PostgREST, so defence-in-depth. | M |

## P5 — Cost and waste

| # | ID | What is wrong | Effort |
|---|-----|---------------|--------|
| 26 | IMP-139 | Unresolvable placeholder names are re-fetched by a 15-minute cron forever, at 12s/call on the shared per-IP GotSport WAF budget, and the pool cannot drain below its unresolvable residue. | M |
| 27 | IMP-144 | No GotSport resolver throttles, retries, or identifies itself, against a WAF-fronted endpoint family, and a 403 lockout is indistinguishable from a genuine 404. | M |
| 28 | IMP-105 | ~55 of 150 minutes of the weekly ranking run is two row-batch writers. | L |
| 29 | IMP-131 | `find_discovery_teams` scans ~3M game rows every Sunday to recompute two booleans a stored column now holds — and holds more correctly, since it resolves merges. | S |
| 30 | IMP-170 | When the ZenRows job POST times out the run reports nothing to recover, but the job may be accepted and billing. The vendor OpenAPI contradicts the spec's premise twice; correct the spec in the same change. | S |
| 31 | IMP-186 | The open-invoice fetch auto-paginates every unpaid invoice ever, on a `force-dynamic` page with a Refresh link. A date floor is the wrong remedy here — the entry explains why. | S |

## P6 — Latent: correct today, wrong the moment conditions change

Zero or near-zero rows affected right now. Each entry records its own measurement.

IMP-150 (blank `state_code` decided as a fill but unwritable, retried forever) ·
IMP-194 (a reverted approval regrants operator authority if the value returns) ·
IMP-166 (a cached probe answer is not bound to the alias it was bought through) ·
IMP-151 (a deprecated team logs a false "no stored state" into the permanent ledger) ·
IMP-149 (GotSport labels aged-out teams U21 and the chain creates them) ·
IMP-147 (2,937 teams stored outside the boarded cohorts — three populations, only one tractable) ·
IMP-110 (audit the `teams.birth_year` rows the old dashboard write stamped) ·
IMP-145 (the repo asserts two contradictory readings of `display_age_group`) ·
IMP-148 (an opponent's cohort can be persisted as the team's own) ·
IMP-159 (an interrupted probe run loses the calls in flight) ·
IMP-162 (a correction leaves `teams.state` contradicting the code beside it — 158 rows) ·
IMP-156 (the tier provenance string is built in SQL as well as Python, unpinned) ·
IMP-182 (an injected Supabase client is only honoured when env vars are also set) ·
IMP-195 (the Sunday jobs cannot tell the activity refresh failed — the refresh is green
again as of 2026-09-07, so its own trigger has not fired) ·
IMP-175 (cancel the in-flight batch when a walk is blocked — the entry concludes the
exposure is a handful of pages and may not be worth it)

## P7 — Tests that cannot fail

IMP-121 (ComparePanel: all ten team-name call sites are unfalsifiable, and the fixtures
differ only by a suffix no assertion reads — cheapest of these) · IMP-155 (`--team
--execute` has both its guards and no test of either; two mutations survive the whole
suite) · IMP-120 (RankingsTable, 648 lines, zero tests; the virtualizer renders zero rows
under happy-dom so the obvious test is not writable as-is) · IMP-122 (ten infographics
modules, zero tests; the five preview components are assertable today) · IMP-118 (four
files hand-roll the same React mount/unmount lifecycle, one from a removed import path)

## P8 — The same code in several places

IMP-167 (7 copies of the GotSport provider lookup — was noted as 5) · IMP-129 (3 copies of
the scrape-log bulk writer) · IMP-126 (the `unknown_` predicate has 3 definitions and the
frontend one already drifted) · IMP-142 (7 hand-copied GotSport resolvers, 2 reading keys
the endpoint has never returned — measured impact is 6 rows, so this is consistency, not
impact) · IMP-171 (7 local chunk helpers plus 14 inlined loops) · IMP-130 (the
scrape-eligibility rule pasted into 3 RPCs, held together by a drift test) · IMP-119
(remove `has_modular11_alias` end-to-end) · IMP-111 (a third age→birth-year derivation) ·
IMP-116 (four rank-delta badges, each picking its own sign semantics) · IMP-123
(ScopedTeamSelector is a near-clone that pitfall 10's grep misses) · IMP-177 (two GotSport
event walkers sharing no code) · IMP-181 (two callers independently pick 8 workers) ·
IMP-174 (the CLI writes a `roster.json` nothing reads) · IMP-187 (subscription items read
as a list in two places and as `data[0]` in two others) · IMP-108 (four loose persistence
kwargs that three callers already disagreed about)

## P9 — Docs, DX, UX polish

IMP-109 · IMP-112 · IMP-125 · IMP-124 · IMP-157 · IMP-158 · IMP-183 · IMP-178 · IMP-176 ·
IMP-115 · IMP-113

## P10 — Blocked on something outside the repo

IMP-190 (needs the current season id from the league organizers) · IMP-191 (Demosphere
publishes no scores yet; re-check after 2026-09-12) · IMP-188 and IMP-189 (both worth
revisiting only alongside a better churn estimate, per their own entries)

## Deferred — leave alone

IMP-179 (autosave a scraped seeding run) · IMP-192 (skip operator-decided teams in the
paid probe list)

---

## Suggested first pass

Items 1, 3, 4, 6 and 17–23 are all **S** and independent of each other — eleven
single-file fixes, each its own PR, no migrations, no shared blast radius. That
clears the whole of P3 plus four of the seven P1 entries.

Items 2, 5 and 7 are the ones that need real work and would change what users see.
